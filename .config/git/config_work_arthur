# vi:syntax=.gitconfig:
# This config is only used by arthur
# Seperated since arthur does not follow "include" directives when mounting, resulting in
# missing files and other weirdness.

[user]
    name = Andreas Palm
    email = andreas@zimpler.com

[url "ssh://git@github.com/zimpler"]
    insteadof = https://github.com/zimpler
    insteadof = https://github.com/Zimpler

[url "ssh://git@gitlab.zimpler.com"]
	insteadof = https://gitlab.zimpler.com
