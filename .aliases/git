alias git-remote-branch="git push origin master:refs/heads/$1 && git fetch origin && git branch --track $1 origin/$1 && git checkout $1"
alias git-branch="git branch | grep \* | cut -d ' ' -f2"
alias git-undo="git reset --soft HEAD~1"
